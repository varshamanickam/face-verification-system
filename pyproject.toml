[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "face-verification-system"
version = "0.1.0"
description = "Face verification system"
requires-python = ">=3.10"
dependencies = [
    "numpy"
]

[tool.setuptools]
packages = ["src"]